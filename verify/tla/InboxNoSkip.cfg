\* The same inbox taking only the oldest row, without stepping over one whose
\* dependencies are not processed.  TLC must find EventuallyProcessed
\* violated: a message that arrives before its dependency blocks the
\* partition, dependency included.
SPECIFICATION Spec
CONSTANTS
  m1 = m1
  m2 = m2
  m3 = m3
  w1 = w1
  w2 = w2
  None = None
  Msgs <- MCMsgs
  Deps <- MCDeps
  Arrives <- MCArrivesAll
  Workers <- MCWorkers
  Dispatchers <- MCDispatchers
  MaxCrashes = 0
  SkipIneligible = FALSE
PROPERTIES
  EventuallyProcessed
