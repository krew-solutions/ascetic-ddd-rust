\* The same inbox without setting aside a message whose dependencies are not
\* processed: the head of the queue waits in place.  TLC must find
\* EventuallyProcessed violated: a message that arrives before its dependency
\* holds the partition, dependency included.
SPECIFICATION Spec
CONSTANTS
  m1 = m1
  m2 = m2
  m3 = m3
  w1 = w1
  w2 = w2
  None = None
  Msgs <- MCMsgs
  Deps <- MCDeps
  Arrives <- MCArrivesAll
  Slots <- MCSlots
  MaxCrashes = 0
  Poison <- MCNoPoison
  MaxAttempts = 0
  BlockOnBackoff = TRUE
  AtomicWait = TRUE
  MaxAdmin = 0
  WaitsOnDependencies = FALSE
  WaitExpires = FALSE
PROPERTIES
  EventuallyProcessed
