\* The bridge acknowledges on delivery and stores afterwards: the Go channel
\* API's shape.  TLC must find a committed message that is never processed:
\* acknowledged, then the process dies before the store.
SPECIFICATION Spec
CONSTANTS
  m1 = m1
  m2 = m2
  m3 = m3
  u1 = u1
  u2 = u2
  w1 = w1
  v1 = v1
  v2 = v2
  None = None
  Msgs <- MCOneMsg
  Uris <- MCOneUri
  SrcWorkers <- MCSrcWorkers
  DstWorkers <- MCOneDst
  DstDispatchers <- MCOneDispatcher
  BatchSize = 1
  MaxCrashes = 1
  StoreBeforeAck = FALSE
PROPERTIES
  EventuallyProcessed
